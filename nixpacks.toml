[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['NITRO_PRESET=node-server npm run build']

[start]
cmd = 'node .output/server/index.mjs'
port = 3000
